version: 0.2

phases:
  build:
    commands:
      - zip -r deployment_package.zip lambda_function.py
      - aws lambda update-function-code --function-name lambda-trabajo-final --zip-file fileb://deployment_package.zip